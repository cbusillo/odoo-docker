# Temporary Odoo 19.0 Python 3.13 requirement overrides.
# Keep these at the minimum fixed versions reported by Trivy, not broad latest
# releases. scripts/check-requirements-overrides.py fails CI once upstream Odoo
# catches up so this file can be removed instead of silently downgrading packages.
cbor2==5.9.0
cryptography==46.0.5
lxml==6.1.0
Pillow==12.2.0
pyopenssl==26.0.0
urllib3==2.6.3
Werkzeug==3.1.6
